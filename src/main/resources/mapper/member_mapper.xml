<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.dao.MemberDao">
	<insert id="insert">
		insert into member(username, password, email, role, levels, join_day, is_lock, failed_attempts, profile)
		values (#{username}, #{password}, #{email}, #{role}, #{level}, #{joinDay}, #{isLock}, #{failedAttempts}, #{profile})
	</insert>
</mapper>